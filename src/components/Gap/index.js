import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const Gap = ({ width, height, flex }) => {
    return (
        <View style={{ width, height, flex }} />
    )
}

export default Gap

const styles = StyleSheet.create({})